<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xavidev.phonehealth">

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneHealth">
        <activity
            android:name=".ui.activities.info.AppInfoActivity"
            android:exported="false"
            android:label="@string/title_activity_app_info"
            android:theme="@style/Theme.PhoneHealth.NoActionBar" />
        <activity
            android:name=".ui.activities.mainMenu.MainMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>